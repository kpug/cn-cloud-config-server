server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kpug/cn-configuration-repo
#          basedir: target/config

---

spring:
  profiles: local